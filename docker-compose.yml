services:
  md-converter:
    build: .
    ports:
      - "5000:5000"
    environment:
      APP_HOST: "0.0.0.0"
      APP_PORT: "5000"
      FLASK_DEBUG: "0"
      MAX_UPLOAD_MB: "200"
      PDF_CJK_FONT: "Noto Serif CJK SC"
    restart: unless-stopped
